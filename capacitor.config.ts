import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sergic.app',
  appName: 'sergic-app',
  webDir: 'dist/sergic-app/browser'
};

export default config;
